{
  "scripts": {
    "start": "yarn dev:start",
    "dev:start": "nodemon --ignore lib,files --exec babel-node src/server/index.js",
    "prod:build": "rimraf lib && babel src -d lib --ignore .test.js",
    "test": "jest --coverage"
  },
  "dependencies": {
    "aws-sdk": "^2.100.0",
    "ejs": "^2.5.7",
    "express": "^4.15.4",
    "multer": "^1.3.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-jest": "^20.0.3",
    "babel-preset-env": "^1.6.0",
    "cross-env": "^5.0.5",
    "jest": "^20.0.4",
    "rimraf": "^2.6.1"
  }
}
